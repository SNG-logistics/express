<!doctype html>
<html lang="th">

<head>
  <meta charset="utf-8">
  <title>เข้าสู่ระบบ - SNG Logistics</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <link rel="stylesheet" href="/css/luxury.css">
</head>

<body class="min-h-screen w-full font-['Prompt'] text-slate-100 bg-slate-900 overflow-hidden relative">
  <div class="absolute inset-0 pointer-events-none">
    <div class="absolute top-[-20%] left-[-10%] w-[50%] h-[50%] bg-amber-500/15 rounded-full blur-[140px]"></div>
    <div class="absolute bottom-[-20%] right-[-10%] w-[55%] h-[55%] bg-indigo-500/15 rounded-full blur-[140px]"></div>
    <div class="absolute inset-0 bg-gradient-to-br from-slate-950/70 via-slate-900/60 to-slate-950/80"></div>
  </div>

  <main class="relative z-10 min-h-screen flex items-center justify-center px-4">
    <div class="w-full max-w-md">
      <div class="glass-panel-dark p-8 md:p-10 rounded-3xl border border-white/10 shadow-2xl relative overflow-hidden">

        <div class="text-center mb-8">
          <div class="mb-4 flex justify-center">
            <img src="/images/logo.png" alt="SNG Logo" class="h-20 w-auto object-contain">
          </div>
          <h1 class="text-2xl font-bold text-white tracking-tight mb-1">ยินดีต้อนรับ</h1>
          <p class="text-slate-400 text-sm">เข้าสู่ระบบจัดการขนส่ง SNG Logistics</p>
        </div>

        <% if (flash) { %>
          <div
            class="mb-6 p-4 rounded-xl border flex items-start gap-3 <%= flash.type === 'error' ? 'bg-red-500/10 border-red-500/20 text-red-200' : 'bg-emerald-500/10 border-emerald-500/20 text-emerald-200' %>">
            <i
              class="<%= flash.type === 'error' ? 'fa-solid fa-circle-exclamation mt-1' : 'fa-solid fa-circle-check mt-1' %>"></i>
            <div class="text-sm">
              <p class="font-bold uppercase tracking-wider text-xs mb-1">
                <%= flash.type==='error' ? 'ข้อผิดพลาด' : 'สำเร็จ' %>
              </p>
              <%= flash.message %>
            </div>
          </div>
          <% } %>

            <form method="post" action="/login" class="space-y-5">
              <div class="space-y-1">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                <label class="text-xs font-bold text-slate-400 uppercase tracking-wider">ชื่อผู้ใช้</label>
                <div class="relative">
                  <input name="username"
                    class="w-full bg-slate-900/50 border border-slate-700 rounded-xl px-4 py-3 pl-11 text-white focus:border-amber-500 focus:ring-1 focus:ring-amber-500 transition-all outline-none"
                    placeholder="Username" required autocomplete="off">
                  <i class="fa-regular fa-user absolute left-4 top-1/2 -translate-y-1/2 text-slate-500"></i>
                </div>
              </div>

              <div class="space-y-1">
                <label class="text-xs font-bold text-slate-400 uppercase tracking-wider">รหัสผ่าน</label>
                <div class="relative">
                  <input name="password" type="password"
                    class="w-full bg-slate-900/50 border border-slate-700 rounded-xl px-4 py-3 pl-11 text-white focus:border-amber-500 focus:ring-1 focus:ring-amber-500 transition-all outline-none"
                    placeholder="Password" required>
                  <i class="fa-solid fa-lock absolute left-4 top-1/2 -translate-y-1/2 text-slate-500"></i>
                </div>
              </div>

              <button
                class="w-full py-3.5 rounded-xl bg-amber-500 hover:bg-amber-600 text-white font-bold shadow-lg shadow-amber-900/20 transition-all flex items-center justify-center gap-2 mt-4">
                <span>เข้าสู่ระบบ</span>
                <i class="fa-solid fa-arrow-right text-sm"></i>
              </button>
            </form>

            <div class="mt-8 text-center border-t border-white/5 pt-6">
              <p class="text-xs text-slate-600">© 2025 SNG Logistics. All rights reserved.</p>
            </div>
      </div>
    </div>
  </main>
</body>

</html>