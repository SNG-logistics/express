<div class="max-w-7xl mx-auto animate-fade-in space-y-8">

    <!-- Header Section -->
    <div class="glass-panel-dark card-elevated rounded-3xl p-8 relative overflow-hidden border border-white/5">
        <div class="absolute top-0 right-0 p-8 opacity-5 pointer-events-none">
            <i class="fa-solid fa-box-open text-9xl text-indigo-500 transform rotate-12"></i>
        </div>

        <div class="relative z-10 flex flex-col lg:flex-row lg:items-start lg:justify-between gap-6">
            <div class="space-y-4">
                <div>
                    <div class="flex items-center gap-3 mb-2">
                        <span
                            class="px-3 py-1 rounded-full text-[10px] font-bold uppercase tracking-widest bg-slate-800 text-slate-400 border border-slate-700">
                            Order Detail
                        </span>
                        <span
                            class="px-3 py-1 rounded-full text-[10px] font-bold uppercase tracking-widest <%= order.direction === 'TH_TO_LA' ? 'bg-indigo-500/10 text-indigo-400 border border-indigo-500/20' : 'bg-amber-500/10 text-amber-400 border border-amber-500/20' %>">
                            <%= order.direction==='TH_TO_LA' ? 'TH 🇹🇭 → LA 🇱🇦' : 'LA 🇱🇦 → TH 🇹🇭' %>
                        </span>
                    </div>
                    <h1 class="text-4xl md:text-5xl font-black text-white tracking-tight leading-tight">
                        <%= order.job_no %>
                    </h1>
                    <p class="text-slate-400 mt-2 font-mono text-sm flex items-center gap-2">
                        <i class="fa-regular fa-clock"></i>
                        Created: <%= new Date(order.created_at).toLocaleString('th-TH') %>
                    </p>
                </div>

                <!-- Horizontal Action Buttons -->
                <div class="flex flex-wrap items-center gap-3 pt-2">
                    <a href="/orders/<%= order.id %>/waybill" target="_blank"
                        class="cta-ghost text-xs font-bold px-4 py-2 rounded-lg flex items-center gap-2">
                        <i class="fa-solid fa-print"></i> Waybill
                    </a>
                    <% if (order.status==='NEW' || order.status==='RECEIVED_WH_TH' || order.status==='RECEIVED_WH_LA' )
                        { %>
                        <a href="/orders/<%= order.id %>/edit"
                            class="cta-ghost text-xs font-bold px-4 py-2 rounded-lg flex items-center gap-2">
                            <i class="fa-solid fa-pen"></i> แก้ไข
                        </a>
                        <% } %>
                </div>
            </div>

            <!-- Status Badge (Responsive) -->
            <div class="flex flex-col items-start lg:items-end gap-3">
                <div class="px-6 py-4 rounded-2xl bg-white/5 border border-white/10 backdrop-blur-md shadow-2xl">
                    <p class="text-[10px] uppercase font-bold text-slate-500 tracking-wider mb-1 text-right">Current
                        Status</p>
                    <div
                        class="text-2xl md:text-3xl font-black text-white whitespace-normal break-words max-w-[280px] text-right leading-tight">
                        <%= order.status %>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <% if (error) { %>
        <div
            class="p-4 rounded-xl bg-red-500/10 text-red-200 border border-red-500/20 flex items-center gap-3 animate-pulse-once shadow-lg">
            <i class="fa-solid fa-circle-exclamation text-xl"></i>
            <%= error %>
        </div>
        <% } %>

            <!-- Info Grids -->
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
                <!-- Card 1 -->
                <div
                    class="glass-panel-dark card-elevated p-6 rounded-3xl border border-white/5 flex flex-col justify-between group hover:border-indigo-500/30 transition-colors">
                    <div>
                        <p class="text-[10px] font-bold text-slate-500 uppercase tracking-widest mb-2">Freight Cost</p>
                        <h3 class="text-3xl font-black text-white">฿<%= Number(order.price_amount).toLocaleString() %>
                        </h3>
                    </div>
                    <div class="mt-4 pt-4 border-t border-white/5 flex items-center gap-2 text-xs text-slate-400">
                        <i class="fa-solid fa-coins text-indigo-400"></i> ค่าขนส่ง
                    </div>
                </div>

                <!-- Card 2 -->
                <div
                    class="glass-panel-dark card-elevated p-6 rounded-3xl border border-white/5 flex flex-col justify-between group hover:border-emerald-500/30 transition-colors">
                    <div>
                        <p class="text-[10px] font-bold text-slate-500 uppercase tracking-widest mb-2">COD Amount</p>
                        <h3 class="text-3xl font-black text-emerald-400">฿<%= Number(order.cod_amount).toLocaleString()
                                %>
                        </h3>
                    </div>
                    <div class="mt-4 pt-4 border-t border-white/5 flex items-center gap-2 text-xs text-slate-400">
                        <i class="fa-solid fa-hand-holding-dollar text-emerald-400"></i> เก็บเงินปลายทาง
                    </div>
                </div>

                <!-- Card 3 -->
                <div
                    class="glass-panel-dark card-elevated p-6 rounded-3xl border border-white/5 flex flex-col justify-between group hover:border-amber-500/30 transition-colors">
                    <div>
                        <p class="text-[10px] font-bold text-slate-500 uppercase tracking-widest mb-2">Weight / Size</p>
                        <div class="flex items-baseline gap-2">
                            <h3 class="text-2xl font-black text-white">
                                <%= order.declared_weight || '-' %> <span class="text-sm text-slate-500">kg</span>
                            </h3>
                        </div>
                        <p class="text-sm text-slate-400 font-mono mt-1">
                            <%= order.declared_size || '-' %>
                        </p>
                    </div>
                    <div class="mt-4 pt-4 border-t border-white/5 flex items-center gap-2 text-xs text-slate-400">
                        <i class="fa-solid fa-scale-balanced text-amber-400"></i> น้ำหนัก/ขนาด
                    </div>
                </div>

                <!-- Card 4 -->
                <div
                    class="glass-panel-dark card-elevated p-6 rounded-3xl border border-white/5 flex flex-col justify-between group hover:border-blue-500/30 transition-colors">
                    <div>
                        <p class="text-[10px] font-bold text-slate-500 uppercase tracking-widest mb-2">Service Type</p>
                        <h3 class="text-xl font-bold text-white uppercase">
                            <%= order.service_type || 'General' %>
                        </h3>
                        <% if (order.requires_customs) { %>
                            <span
                                class="inline-flex mt-2 items-center px-2 py-0.5 rounded text-[10px] font-bold bg-blue-500/10 text-blue-400 border border-blue-500/20">
                                <i class="fa-solid fa-shield-halved mr-1"></i> Customs
                            </span>
                            <% } %>
                    </div>
                    <div class="mt-4 pt-4 border-t border-white/5 flex items-center gap-2 text-xs text-slate-400">
                        <i class="fa-solid fa-truck text-blue-400"></i> ประเภทบริการ
                    </div>
                </div>

                <!-- Card 5: Parcel Image (Conditional) -->
                <% if (order.image_path) { %>
                    <div
                        class="glass-panel-dark card-elevated p-2 rounded-3xl border border-white/5 flex flex-col justify-between group hover:border-indigo-500/30 transition-colors overflow-hidden relative">
                        <img src="<%= order.image_path %>" alt="Parcel Image"
                            class="w-full h-40 object-cover rounded-2xl cursor-pointer hover:scale-105 transition-transform duration-500"
                            onclick="window.open(this.src, '_blank')">
                        <div
                            class="absolute bottom-2 right-2 bg-black/60 px-2 py-1 rounded text-[10px] text-white backdrop-blur">
                            <i class="fa-solid fa-camera"></i>
                        </div>
                    </div>
                    <% } %>
            </div>

            <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">

                <!-- Left Column: Partners & Actions -->
                <div class="lg:col-span-2 space-y-8">

                    <!-- Partners -->
                    <div class="glass-panel-dark card-elevated rounded-3xl p-6 border border-white/5">
                        <h3 class="text-lg font-bold text-white mb-6 flex items-center gap-2">
                            <i class="fa-solid fa-users text-indigo-400"></i> ข้อมูลคู่ค้า (Partners)
                        </h3>
                        <div class="grid md:grid-cols-2 gap-8">
                            <!-- Sender -->
                            <div class="relative bg-white/5 rounded-2xl p-5 border border-white/5">
                                <div
                                    class="absolute -top-3 left-4 px-2 bg-slate-800 text-[10px] font-bold text-slate-400 uppercase tracking-wider border border-slate-700 rounded">
                                    Sender (ผู้ส่ง)
                                </div>
                                <div class="mt-2">
                                    <p class="text-lg font-bold text-white">
                                        <%= order.sender_name || '-' %>
                                    </p>
                                    <p class="text-sm text-slate-400">
                                        <%= order.sender_phone || '-' %>
                                    </p>
                                    <% if (order.sender_address) { %>
                                        <p
                                            class="text-xs text-slate-500 mt-2 pt-2 border-t border-white/5 leading-relaxed">
                                            <%= order.sender_address %>
                                        </p>
                                        <% } %>
                                </div>
                            </div>

                            <!-- Receiver -->
                            <div class="relative bg-white/5 rounded-2xl p-5 border border-white/5">
                                <div
                                    class="absolute -top-3 left-4 px-2 bg-slate-800 text-[10px] font-bold text-slate-400 uppercase tracking-wider border border-slate-700 rounded">
                                    Receiver (ผู้รับ)
                                </div>
                                <div class="mt-2">
                                    <p class="text-lg font-bold text-white">
                                        <%= order.receiver_name || '-' %>
                                    </p>
                                    <p class="text-sm text-slate-400">
                                        <%= order.receiver_phone || '-' %>
                                    </p>
                                    <% if (order.receiver_address) { %>
                                        <p
                                            class="text-xs text-slate-500 mt-2 pt-2 border-t border-white/5 leading-relaxed">
                                            <%= order.receiver_address %>
                                        </p>
                                        <% } %>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Action Panel -->
                    <div class="glass-panel-dark card-elevated rounded-3xl p-6 border border-white/5">
                        <h3 class="text-lg font-bold text-white mb-6 flex items-center gap-2">
                            <i class="fa-solid fa-bolt text-amber-400"></i> จัดการสถานะ (Actions)
                        </h3>

                        <div class="flex flex-wrap gap-4">
                            <!-- Receive Order -->
                            <% const canReceive=(['admin', 'manager' ].includes(user.role)) ||
                                (user.role==='thai_warehouse' && order.direction==='TH_TO_LA' ) ||
                                (user.role==='lao_warehouse' && order.direction==='LA_TO_TH' ); %>
                                <% if ((['NEW'].includes(order.status) || (!order.status)) && canReceive) { %>
                                    <form action="/orders/<%= order.id %>/receive" method="post">
                                        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                                        <button
                                            class="bg-indigo-600 hover:bg-indigo-500 text-white px-6 py-3 rounded-xl font-bold shadow-lg shadow-indigo-900/20 transition-all hover:-translate-y-1 flex items-center gap-2">
                                            <i class="fa-solid fa-inbox"></i> รับพัสดุเข้าคลัง
                                        </button>
                                    </form>
                                    <% } %>

                                        <!-- Start Crossing -->
                                        <% if (['ON_TRUCK_BORDER'].includes(order.status)) { %>
                                            <form action="/orders/<%= order.id %>/crossing" method="post">
                                                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                                                <button
                                                    class="bg-amber-600 hover:bg-amber-500 text-white px-6 py-3 rounded-xl font-bold shadow-lg shadow-amber-900/20 transition-all hover:-translate-y-1 flex items-center gap-2">
                                                    <i class="fa-solid fa-truck-moving"></i> เริ่มข้ามแดน
                                                </button>
                                            </form>
                                            <% } %>

                                                <!-- Arrived Border -->
                                                <% if (['CROSSING_BORDER'].includes(order.status)) { %>
                                                    <form action="/orders/<%= order.id %>/arrived-border" method="post">
                                                        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                                                        <button
                                                            class="bg-emerald-600 hover:bg-emerald-500 text-white px-6 py-3 rounded-xl font-bold shadow-lg shadow-emerald-900/20 transition-all hover:-translate-y-1 flex items-center gap-2">
                                                            <i class="fa-solid fa-flag-checkered"></i> ถึงด่านปลายทาง
                                                        </button>
                                                    </form>
                                                    <% } %>

                                                        <!-- Arrived Destination WH -->
                                                        <% if (['ARRIVED_BORDER_WH'].includes(order.status)) { %>
                                                            <form action="/orders/<%= order.id %>/arrived-dest"
                                                                method="post">
                                                                <input type="hidden" name="_csrf"
                                                                    value="<%= csrfToken %>">
                                                                <button
                                                                    class="bg-indigo-600 hover:bg-indigo-500 text-white px-6 py-3 rounded-xl font-bold shadow-lg shadow-indigo-900/20 transition-all hover:-translate-y-1 flex items-center gap-2">
                                                                    <i class="fa-solid fa-warehouse"></i>
                                                                    ถึงโกดังปลายทาง
                                                                </button>
                                                            </form>
                                                            <% } %>

                                                                <!-- Start Delivery -->
                                                                <% if (['AT_DEST_WH'].includes(order.status)) { %>
                                                                    <form action="/orders/<%= order.id %>/delivery"
                                                                        method="post">
                                                                        <input type="hidden" name="_csrf"
                                                                            value="<%= csrfToken %>">
                                                                        <button
                                                                            class="bg-blue-600 hover:bg-blue-500 text-white px-6 py-3 rounded-xl font-bold shadow-lg shadow-blue-900/20 transition-all hover:-translate-y-1 flex items-center gap-2">
                                                                            <i class="fa-solid fa-motorcycle"></i>
                                                                            ออกส่งพัสดุ
                                                                        </button>
                                                                    </form>
                                                                    <% } %>

                                                                        <!-- Mark Delivered -->
                                                                        <% if
                                                                            (['OUT_FOR_DELIVERY'].includes(order.status))
                                                                            { %>
                                                                            <form
                                                                                action="/orders/<%= order.id %>/delivered"
                                                                                method="post">
                                                                                <input type="hidden" name="_csrf"
                                                                                    value="<%= csrfToken %>">
                                                                                <button
                                                                                    class="bg-emerald-600 hover:bg-emerald-500 text-white px-6 py-3 rounded-xl font-bold shadow-lg shadow-emerald-900/20 transition-all hover:-translate-y-1 flex items-center gap-2">
                                                                                    <i
                                                                                        class="fa-solid fa-check-double"></i>
                                                                                    ส่งถึงมือลูกค้าแล้ว
                                                                                </button>
                                                                            </form>
                                                                            <% } %>

                                                                                <!-- Close Order -->
                                                                                <% if
                                                                                    (['DELIVERED','COD_COLLECTED'].includes(order.status))
                                                                                    { %>
                                                                                    <form
                                                                                        action="/orders/<%= order.id %>/close"
                                                                                        method="post">
                                                                                        <input type="hidden"
                                                                                            name="_csrf"
                                                                                            value="<%= csrfToken %>">
                                                                                        <button
                                                                                            class="bg-slate-700 hover:bg-slate-600 text-white px-6 py-3 rounded-xl font-bold shadow-lg transition-all hover:-translate-y-1 flex items-center gap-2">
                                                                                            <i
                                                                                                class="fa-solid fa-lock"></i>
                                                                                            ปิดงาน (Close Order)
                                                                                        </button>
                                                                                    </form>
                                                                                    <% } %>
                        </div>
                    </div>
                </div>

                <!-- Financials Panel (Restored) -->
                <div class="glass-panel-dark card-elevated rounded-3xl p-6 border border-white/5 mt-8">
                    <h3 class="text-lg font-bold text-white mb-6 flex items-center gap-2">
                        <i class="fa-solid fa-receipt text-emerald-400"></i> การเงิน (Financials)
                    </h3>

                    <!-- Payments List -->
                    <div class="space-y-4 mb-6">
                        <% if (payments && payments.length> 0) { %>
                            <% payments.forEach(p=> { %>
                                <div
                                    class="bg-white/5 rounded-xl p-4 border border-white/5 flex items-center justify-between group hover:border-emerald-500/20 transition-all">
                                    <div class="flex items-center gap-4">
                                        <div
                                            class="w-10 h-10 rounded-full bg-emerald-500/10 flex items-center justify-center text-emerald-400">
                                            <i class="fa-solid fa-money-bill"></i>
                                        </div>
                                        <div>
                                            <p class="font-bold text-white">
                                                <%= p.type.toUpperCase() %>
                                            </p>
                                            <p class="text-xs text-slate-400">
                                                <%= new Date(p.created_at).toLocaleString('th-TH') %>
                                            </p>
                                            <% if (p.ref_no) { %>
                                                <p class="text-xs text-slate-500 italic">"<%= p.ref_no %>"</p>
                                                <% } %>
                                        </div>
                                    </div>
                                    <div class="text-right">
                                        <p class="font-bold text-emerald-400 text-lg">฿<%=
                                                Number(p.amount).toLocaleString() %>
                                        </p>
                                        <form action="/orders/<%= order.id %>/payments/<%= p.id %>/delete" method="post"
                                            class="inline-block mt-1"
                                            onsubmit="return confirm('Delete this payment?');">
                                            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                                            <button type="submit"
                                                class="text-[10px] text-red-400 hover:text-red-300 flex items-center gap-1 opacity-0 group-hover:opacity-100 transition-opacity">
                                                <i class="fa-solid fa-trash"></i> DELETE
                                            </button>
                                        </form>
                                    </div>
                                </div>
                                <% }) %>
                                    <% } else { %>
                                        <div
                                            class="text-center py-8 text-slate-500 bg-white/5 rounded-xl border border-dashed border-white/10">
                                            <i class="fa-solid fa-receipt text-2xl mb-2 opacity-50"></i>
                                            <p>No expenses recorded</p>
                                        </div>
                                        <% } %>
                    </div>

                    <!-- Add Payment Form -->
                    <form action="/orders/<%= order.id %>/payments" method="post"
                        class="bg-white/5 rounded-xl p-5 border border-white/5">
                        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                        <p class="text-xs font-bold text-slate-400 uppercase tracking-widest mb-4">Add Expense / Payment
                        </p>

                        <div class="grid grid-cols-1 md:grid-cols-3 gap-4 mb-4">
                            <div>
                                <label class="block text-[10px] text-slate-500 uppercase font-bold mb-1">Type</label>
                                <select name="type"
                                    class="w-full bg-slate-800 text-white text-sm rounded-lg border border-slate-700 px-3 py-2 focus:ring-2 focus:ring-emerald-500 focus:outline-none">
                                    <option value="fee">Fee (ค่าธรรมเนียม)</option>
                                    <option value="freight">Freight (ค่าขนส่ง)</option>
                                    <option value="customs">Customs (ภาษี)</option>
                                    <option value="other">Other (อื่นๆ)</option>
                                </select>
                            </div>
                            <div>
                                <label class="block text-[10px] text-slate-500 uppercase font-bold mb-1">Amount</label>
                                <input type="number" name="amount" step="0.01" required placeholder="0.00"
                                    class="w-full bg-slate-800 text-white text-sm rounded-lg border border-slate-700 px-3 py-2 focus:ring-2 focus:ring-emerald-500 focus:outline-none placeholder-slate-600">
                            </div>
                            <div>
                                <label class="block text-[10px] text-slate-500 uppercase font-bold mb-1">Note</label>
                                <input type="text" name="note" placeholder="Optional"
                                    class="w-full bg-slate-800 text-white text-sm rounded-lg border border-slate-700 px-3 py-2 focus:ring-2 focus:ring-emerald-500 focus:outline-none placeholder-slate-600">
                            </div>
                        </div>

                        <div class="flex justify-end">
                            <button type="submit"
                                class="bg-emerald-600 hover:bg-emerald-500 text-white text-xs font-bold px-4 py-2 rounded-lg shadow-lg hover:shadow-emerald-900/20 transition-all hover:-translate-y-0.5 flex items-center gap-2">
                                <i class="fa-solid fa-plus"></i> Add Expense
                            </button>
                        </div>
                    </form>
                </div>
            </div>

            <!-- Right Column: Timeline -->
            <div class="glass-panel-dark card-elevated rounded-3xl p-6 border border-white/5 h-full">
                <div class="flex items-center justify-between mb-6 pb-4 border-b border-white/5">
                    <h3 class="text-lg font-bold text-white">Timeline</h3>
                    <span class="text-[10px] font-mono text-slate-500">
                        <%= logs.length %> Activites
                    </span>
                </div>

                <div class="relative pl-4 border-l border-slate-800 space-y-8">
                    <% logs.forEach(log=> { %>
                        <div class="relative group">
                            <div
                                class="absolute -left-[21px] top-1.5 w-3 h-3 rounded-full bg-slate-800 border-2 border-slate-600 group-hover:border-amber-500 group-hover:bg-amber-500 transition-colors">
                            </div>
                            <div>
                                <p
                                    class="text-sm font-bold text-slate-200 group-hover:text-amber-400 transition-colors">
                                    <%= log.to_status %>
                                </p>
                                <p class="text-[10px] text-slate-500 font-mono mt-0.5">
                                    <%= new Date(log.action_at).toLocaleString('th-TH') %>
                                </p>
                                <p class="text-xs text-slate-400 mt-1">
                                    by <%= log.username || 'System' %>
                                </p>
                                <% if (log.note) { %>
                                    <div
                                        class="mt-2 text-xs bg-white/5 p-2 rounded text-slate-300 italic border border-white/5">
                                        "<%= log.note %>"
                                    </div>
                                    <% } %>
                            </div>
                        </div>
                        <% }) %>
                            <% if (logs.length===0) { %>
                                <div class="text-xs text-slate-500 italic pl-2">No history logs</div>
                                <% } %>
                </div>
            </div>

</div>
</div>